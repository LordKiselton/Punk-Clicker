# План до релиза «Балаган» (RuStore)

Статус на 2026-07-01. Живой чек-лист «что осталось до публикации».
Связано: [ROADMAP.md](ROADMAP.md) · [MONETIZATION.md](MONETIZATION.md) · [BALANCE.md](BALANCE.md) · [reference-godot-android-build (memory)].

Текущий билд: **v47** (debug-подпись), ветка `feature/feel2`, коммит `a25a0a5`.

---

## Легенда
- 🔴 **Блокер** — без этого нельзя опубликовать/монетизировать.
- 🟡 **Важное** — качество/комплаенс, до релиза.
- 🟢 **Полиш** — желательно, можно апдейтом.
- 👤 = нужно действие/аккаунт от разработчика (вне кода).

---

## 🔴 Блокеры

| # | Пункт | Состояние | Действие |
|---|-------|-----------|----------|
| B1 | **Релизная подпись** | Сейчас debug-cert Godot (CN=Godot), собираем `--export-debug` | Сгенерировать release-keystore, прописать в `export_presets.cfg`, собирать `--export-release` (AAB для RuStore) |
| B2 | **Аккаунт RuStore Developer** 👤 | нет | Регистрация, создание приложения, получение доступа к консоли/биллингу |
| B3 | **Политика конфиденциальности (URL)** 👤 | кнопка-заглушка | Написать + захостить; вставить URL в настройки; заявить в data-safety (реклама + микрофон собирают данные) |
| B4 | **Боевые Yandex ad-unit ID** 👤 | `demo-rewarded-yandex` | Аккаунт РСЯ → реальные ID → заменить в `Monetization.gd` |
| B5 | **Модель монетизации v1 — решение** 👤 | не зафиксирована | Реклама-only ИЛИ +IAP. Если IAP → интеграция **RuStore Billing** (сейчас заглушка) + товары в консоли |
| B6 | **Стор-карточка** 👤 | нет | Скриншоты, описание, категория, **возрастной рейтинг** (анкета), фичер-графика. Иконка ✅ |

## 🟡 Важное

| # | Пункт | Состояние | Действие |
|---|-------|-----------|----------|
| I1 | **Убрать `[ТЕСТ]`-кнопку** + мёртвый reset-код | в сборке | Удалить `dev_boost_to_50`-кнопку из настроек + неиспользуемый `_on_reset_pressed`/`reset_progress` перед релизом |
| I2 | **Калибровка баланса** | боссы-стены на глаз; 130 за ~20 мин | Телеметрия (стадия/время/DPS) → 1 плейтест → тюнинг под ~2–3 ч до потолка. См. [BALANCE.md](BALANCE.md) |
| I3 | **Микрофон (ХОЙ)** — решение | `record_audio` в манифесте всегда | Оставить + объяснить в сторе, ИЛИ запрос только при первом использовании, ИЛИ убрать голос (оставить «держи 5 сек») |
| I4 | **Аналитика** | нет | AppMetrica (Yandex): воронка, D1/D7, доход с рекламы. Без неё — вслепую |
| I5 | **Версия** | `code=1`, `name=0.1` | Поднять к релизу (напр. `code=100`, `name=1.0.0`) |
| I6 | **Размер APK** | ~207 МБ (32+64-бит движок) | Отключить `armeabi-v7a` → размер ~вдвое; проверить охват устройств |
| I7 | **targetSdk** | 34 | Проверить требования RuStore на 2026 (возможно 35), поднять при необходимости |

## 🟢 Полиш

| # | Пункт | Комментарий |
|---|-------|-------------|
| P1 | Дейли-ревард / стрик | Держит ретеншн, дёшево |
| P2 | Пуш-уведомления возврата | «труппа заждалась», «копилка полна» |
| P3 | Онбординг кнопки «Клад» (rewarded) | сейчас нигде не объясняется |
| P4 | SFX-покрытие | проверить, везде ли звуки |
| P5 | Тест производительности на M52 | фреймрейт/батарея/память в долгой сессии |
| P6 | Проактивная реклама «×2 урон боссу» | главный rewarded-момент на стене |
| P7 | Меч/лоадскрин без альфы (RGB) | проверить композицию на вытянутом экране |

---

## Lean v1.0 «не стыдно выложить»
Минимум для публикации = **все 🔴 (B1–B6)** + **I1, I2, I5** (+ желательно I3, I4).
Остальное (🟢) — апдейтами после софт-лонча.

## Порядок работ
1. **Решения дизайнера** (B5 монетизация, I3 микрофон, B6 рейтинг) — разблокируют половину.
2. **Аккаунты** 👤 (B2 RuStore, B4 Yandex) — параллельно, долгий срок.
3. **Код в моей зоне**: I1 (тест-кнопка), I2 (телеметрия), I5 (версия), I6 (размер), B1-пайплайн (жду keystore), вставка URL/ID (B3/B4 по готовности).
4. **Стор**: B3 политика, B6 карточка → тест-трек → публикация.

## Что могу сделать сейчас (без внешнего)
- I1 убрать тест-кнопку/мёртвый код · I5 версия · I6 размер APK · I2 телеметрию · B1 подготовить release-пайплайн.
