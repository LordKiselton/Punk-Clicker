# Тех-требования к арту «Балагана»

> Для генерации/рисовки ассетов под текущий UI ([VISION.md](VISION.md) · [LORE.md](LORE.md)).
> Формат ассетов, чтобы встали без переделок UI.

## Статус арта (2026-08-26)

| Слой | Статус | Где |
|---|---|---|
| Фоны ×5 | **v2 в бою** (`forest` = `r3_no_cross`) | `art/bg/*.png`, паспорта `art/bg/v2/passports.md`, волна РФ `art/bg/v2/passports-rf.md`, архив `art/bg/v1/` |
| Труппа ×10 | **v3 в UI** | `art/troupe/v3/` (рельс); архив `art/troupe/v2/` · боевые `art/troupe/*.png` |
| Клинок (рельс) | **принят** | `art/ui/sword.png` · [паспорт](art/ui/sword.passport.md) |
| Сайдбар | hand-drawn | `art/ui/side/` |
| Таб-бар | текст-only; иконки в бэклоге | `art/ui/tabs/BACKLOG.md` |
| Лоадскрин | есть; **кандидаты key art 2026-08-27** | боевой `art/ui/loading.png`; прогон `art/keyart/` |
| Монстры / боссы | в бою старые; **v2 кандидаты 2026-08-27** | бой `art/enemies/*.png` · [паспорта](art/enemies/passports.md) · [пайплайн](art/enemies/PIPELINE.md) · кандидаты `art/enemies/v2/` (+ `alpha/`) |

### Ближайшие арт/аудио задачи (не срочно)
1. **Перегенерация монстров** — пайплайн зафиксирован: [art/enemies/PIPELINE.md](art/enemies/PIPELINE.md). Кандидаты v2 уже лежат в `art/enemies/v2/` (+ `alpha/`); в бой — после приёмки. Живых id — 15; отдельных спрайтов боссов нет.
2. **SFX UI** — клик кнопки, открытие/закрытие модалки, таб, claim награды, лёгкий fail (см. также пункт SFX в [ROADMAP.md](ROADMAP.md)).
3. **Новый лоадскрин** — композиция под текущий бренд (панк-шут / локация v2), без устаревшего кадра; размер/роль как у «Сплеш-лого» ниже.

## Базовые параметры

- **Дизайн-разрешение игры:** 720×1280 (портрет), растяжение `canvas_items`. Рисуем арт в **2×** под современные экраны (≈1080p+).
- **Движок:** Godot 4.6.1. Импорт PNG/WebP «из коробки», VRAM-компрессия ETC2/ASTC включена.
- **Прозрачность:** все персонажи, враги, иконки, рамки — **PNG-32 с альфа-каналом** (прозрачный фон). Фоны локаций — без альфы (PNG или WebP q85).
- **Лимит размера текстуры:** ≤ 2048 px по большей стороне (совместимость). Бюджет арта в APK: ориентир **до ~50 МБ** суммарно (фоны — в WebP).

## Палитра (фиксируем единый стиль)

| Роль | HEX |
|---|---|
| Фон тёмный (баклажан-чёрный) | `#1a1320` |
| Поверхность/панель | `#241a2c` / рамка `#3a2b44` |
| Кровавый красный (HP, колпак, акцент опасности) | `#b5121b` |
| Балаганный золотой (золото, выделение) | `#e0c341` |
| Болотно-зелёный (труппа/природа) | `#6f9f5a` |
| Текст светлый / приглушённый | `#e8e0ee` / `#9a8fa6` |
| Цвета чисел героев | Голова `#c9a0dc` · Громобой `#5bc0eb` · Ведьма `#6f9f5a` · Путник `#e0a85a` |

## Сколько контента нужно (минимум для релиза)

**Главный принцип жанра:** контент в кликере **переиспользуется петлёй** — игрок проходит одни и те же стадии много раз через prestige. Не нужна бесконечность: нужно, чтобы **первые 1–2 часа выглядели разнообразно**, дальше работает «число-растёт». Полировка **ранних** локаций важнее их количества (локацию 1 увидят десятки раз, локацию 6 — пару раз).

Привязка к балансу: `STAGES_PER_LOCATION=50`, босс каждые 5 стадий, первый prestige на стадии 75 (уже во 2-й локации). За первые prestige-заходы игрок доходит до стадий ~150–250 → это 4–5 локаций. После последней рисованной локации — **цикл с перекраской палитры** (ночь/кровавая луна/зима), числа продолжают расти. Это норма жанра.

| Контент | Минимум (lean v1) | Полная версия | Зачем |
|---|---|---|---|
| **Фоны (локации)** | **3** | 5–6 | покрыть стадии 1–200 и несколько prestige до повтора |
| **Герои (труппа)** | **6**, лучше **8** | 8–12 | «морковки» под трату золота; на 3 тратить нечего → мета пустеет |
| **Рядовые монстры** | 2 на локацию | 3 на локацию | часть — перекраски; **привязать к локации** |
| **Боссы** | 1 на локацию | 1 уникальный на локацию | визуальный «гейт» в конце локации |

**Цель «не стыдно выложить»: 4 фона, 8 героев, ~8 монстров, 4 босса** (~18–22 ассета). Меньше — можно, но игра ощущается демкой.

**Приёмы растянуть арт дёшево:**
- **Перекраски монстров** по локациям (один дизайн = 3–4 врага разной палитрой).
- **Тинт фонов** — день/ночь/кровавая луна из одного фона.
- **Босс = «элитная» версия** рядового: крупнее, другая палитра, аура.
- **Силуэты** для незаработанных героев (уже так в UI).

**Порядок генерации (исторический lean):** герои → фоны → враги по локациям → боссы.  
**Сейчас:** фоны и труппа закрыты v2; следующий крупный арт-прогон — **единый стиль монстров** (см. статус выше).

> Код: враги уже привязаны к локациям. Долг — **визуальная унификация** ассетов, не словарь.

---

## Список ассетов

> Ниже — **полный** перечень-цель (6 локаций). Для первого релиза достаточно lean-минимума из таблицы выше.

### Персонажи и враги (PNG-32, прозрачный фон, в полный рост, по центру кадра)

| Ассет | Размер (px) | Ориентация | Заметки |
|---|---|---|---|
| Шут (герой) | ~500×700 | смотрит **вправо** | поза idle + поза удара (2 кадра) для сочности тапа |
| Союзник ×4 (Голова, Громобой, Ведьма, Путник) | ~300×450 | смотрит **вправо** | idle; стоят слева рядом с Шутом |
| Рядовой враг ×4 на локацию | ~400×400 | смотрит **влево** | idle; по 4 типа на каждую из 6 локаций |
| Босс ×1 на локацию | ~640×640 | смотрит **влево** | крупнее, читаемый силуэт |

> Якорь: ступни внизу кадра по центру по X. Единое направление света (сверху-слева). Толстый контур, плоская заливка — стиль «тёмная книжка-сказка».

### Фоны локаций (боевые 5 + цель 6; ~1080×1920 / 9:16, без альфы)

**В бою:** Лес · Погост · Трактир · Город · Замок — файлы `art/bg/{forest,graveyard,tavern,city,castle}.png` (= копии v2). Паспорта и кандидаты: `art/bg/v2/`.  
**Цель по лору (позже):** +Усадьба / Ярмарка и т.п. — см. [LORE.md](LORE.md).

Композиция: **верхние ~55%** — сцена/атмосфера; **средняя «арена-полоса»** — где стоит враг (не загромождать центр); **нижние ~35%** — притемнить/упростить (поверх ляжет UI-панель и таб-бар, детали не нужны).

### UI (PNG-32 с альфа)

| Ассет | Размер | Заметки |
|---|---|---|
| Иконки валют: монета, череп, бубенец | 128×128 | или оставить шрифтовые иконки |
| Плашка-кнопка (9-slice) | ~96×96 | поля под растяжение (9-slice), «деревянная/каменная» рамка |
| Рамка панели (9-slice) | ~128×128 | для списков прокачки/труппы |
| Иконки таб-бара ×6 (Шут/Труппа/Навыки/Тропа/Лавка/Сказка) | 96×96 | линейные, в стиле |
| Полоса HP: рамка + заливка | ~512×64 | заливка отдельным слоем (тайлится/режется) |

### Иконки приложения (для RuStore и системы)

| Ассет | Размер | Заметки |
|---|---|---|
| Иконка стора/листинга | 512×512 PNG | без прозрачности |
| Adaptive foreground | 432×432 PNG (alpha) | значимое — в центральном «safe» круге ⌀ ~280 |
| Adaptive background | 432×432 PNG | сплошной фон/паттерн |
| Legacy launcher | 192×192 PNG | |
| Сплеш-лого | ~512×512 PNG (alpha) | на тёмном фоне `#1a1320` |

## Иконка приложения (стор + лаунчер)

С **v1.0.4** — портрет панк-шута в стиле Darkest Dungeon (матч врагам/скринам, без текста на иконке).

| Файл | Размер | Назначение |
|------|--------|------------|
| `res://icon/icon_512.png` | 512×512 | `project.godot` `config/icon`, стор/превью |
| `res://icon/icon_192.png` | 192×192 | legacy launcher (`launcher_icons/main_192x192`) |
| `res://icon/icon_fg.png` | 432×432 | adaptive foreground |
| `res://icon/icon_bg.png` | 432×432 | adaptive background (плотный тёмно-красный) |

- Исходник / варианты генерации: `store/icons/` (папка под `.gdignore` у `store/` — в APK не едет)
- В `res://icon/` только четыре боевых PNG выше — не класть `source_*`
- Менять иконку стора и билда одним апдейтом; смена иконки ≠ смена package → прогресс игроков сохраняется

## Папки и имена (snake_case)

```
res://art/
  bg/            forest.png, graveyard.png, tavern.png, city.png, castle.png   # боевые (= v2)
  bg/v1/         архив старых фонов (дев-превью)
  bg/v2/         forest_v2.png … + passports.md, forest_candidates/
  heroes/        jester.png …
  troupe/v2/     knight.png, ratrogue.png, … (10 героев)
  enemies/       zombie.png, werewolf.png, … (общая папка; привязка к локации в коде)
  ui/            loading.png, side/, tabs/BACKLOG.md, panel/btn/bar ассеты
  icons/         coin.png, skull.png, bell.png
```

## Подсказки по AI-генерации

- **Единый стиль:** один «стилевой якорь» (референс-кадр) + фиксированный seed, чтобы герои/враги были в одной вселенной.
- **Прозрачность:** многие генераторы не дают альфу — генерь на сплошном контрастном фоне (напр. чистый `#00ff00`), затем вырезай фон; на выходе — PNG с альфа.
- **Промпт-скелет (персонаж):** «full-body character, centered, facing right, transparent/flat background, flat 2D storybook illustration, dark punk fairy tale, thick ink outline, limited palette (#1a1320 #b5121b #e0c341 #6f9f5a), grim but playful, Russian dark folklore vibe — [кто именно, из LORE.md]».
- **Враги:** то же, но `facing left`, `monster/creature`.
- **Фоны:** `vertical 9:16 background, [локация], atmospheric, darker bottom third for UI, flat 2D storybook, same palette`.
- Согласуем по одному типу за раз, я встрою и проверю на телефоне.

## Как встраиваем

Кладёшь файлы по путям выше → я заменяю векторные заглушки на `TextureRect`/`Sprite2D` в тех же слотах (UI уже спроектирован под эти размеры) и собираю APK. Размеры держим как в таблице — тогда ничего не «поедет».
