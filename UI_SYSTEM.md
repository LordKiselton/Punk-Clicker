# UI/UX-система «Балаган» — ревью и целевая архитектура

> **Снимок старого HUD (до UI+Art апгрейда).** Закон раскладки, нав, токенов и домов сущностей — [UI_CANON.md](UI_CANON.md). Ниже остаётся карта кода «как было» и диагноз; целевой док в §3–5 не применять, если он спорит с каноном (док vs таб-бар — канон взял таб-бар).

> Системный разбор текущего UI и целевая система под рост (квесты, карточки героев, барки, гача/шарды, коллекция, бестиарий). Цель: чтобы каждая новая сущность «вставала в систему», а не клеилась сбоку.

---

## 1. Текущее состояние (карта по коду)

### Экран — постоянный HUD, 720×1280 портрет (Main.tscn)
Сверху вниз:
- **TopBar** (y 36–130): валюты — Золото + Колокола видимы; Черепа/Rate скрыты (резерв).
- **Title** (y 138–252): заголовок, этап, пипсы, полоса босса.
- **Arena** (y 256 … −516): враг, HP-бар, тап-зона, **Клад «▶ ×2»** (верх-право), слой fly-fx.
- **Нижний блок** (фикс, ~440px): **PunkSlot** (−500…−436) → **TroupeRail** (карточки героев, −428…−132) → **ActionBar** (мультипликаторы + наточить, −120…−64).

### Слои модалок (CanvasLayer, z-order)
| z | Слой |
|---|---|
| 40 | Туториал |
| 45 | *(свободно — предлагаю под барки)* |
| 50 | Панк-FX |
| 55 | Босс-оффер (тайминг-критичный) |
| 57 | Афиша (дейли) |
| 58 | Престиж |
| 60 | Настройки |
| 66 | Диалог с портретом |
| 80 | Тост / «Клад»-подсказка |
| 95 | Фейд |
| 100 | Лоадскрин |

### Стиль
- **Палитра:** BG `#1a1320`, SURF `#241a2c`, SURF_BORDER `#3a2b44`, ARENA `#221829`, BLOOD `#b5121b`, GOLD `#e0c341`, GREEN `#6f9f5a`, TXT `#e8e0ee`, MUTED `#9a8fa6`, WOOD `#2a1f12`, WOOD_BORDER `#7a5a1f`, DARK `#120d18`, PORTRAIT_BG `#1d1525`. + акцент-цвет на каждого героя.
- **Шрифт-шкала:** F_TITLE 34, F_NUM 32, F_RES 30, F_BOSS 42, F_BODY 26, F_SUB 22, F_SMALL 18 (+ F_DMG/CRIT/PASSIVE). НО много ad-hoc размеров мимо шкалы (72/88/130/150/64/66/15).
- **Фабрики:** `_flat(bg,border,radius,bw,margin)`, `_style_button(b,bg,border,fg)`, `_lab(l,fs,color)`.
- **Габариты:** модалка `box=560px`, карточка/мульт-кнопка `166px`, кнопки h52/56/64, pill radius 999.

### Точки входа в окна
Разрозненные: шестерёнка (настройки), кнопка Афиши, кнопка престижа, Клад (арена), авто (босс-оффер, welcome, диалоги).

---

## 2. Диагноз — что ломается под рост

- **P1. Нет модального менеджера/очереди.** Каждая модалка — свой слой + свой диммер. При совпадении (welcome + афиша + review-prompt + босс-оффер) они стекаются, диммеры складываются, перебивают друг друга. ← главный риск.
- **P2. Нет навигационной оболочки.** Входы разрозненны; новым сущностям (квесты, карточки, гача, коллекция, бестиарий) некуда системно встать — каждая лепится своей кнопкой.
- **P3. Рассогласования.** Диммеры 0.55/0.65/0.7; куча font-size мимо шкалы. По мелочи, но копится в «непричёсанность».
- **P4. Нижняя зона переполнена.** PunkSlot+TroupeRail+ActionBar заняли ~440px. Барки и индикатор квестов конкурируют за место.
- **P5. Валютная строка вырастет.** Золото + Колокола + Шарды (гача) — TopBar надо готовить к росту.

---

## 3. Целевая система (концепции)

### 3.1 Модальный менеджер + очередь (решает P1)
Единый менеджер: **один диммер, одна открытая модалка, приоритетная очередь**.
Приоритеты: `welcome > boss-offer > gacha-reveal > review-prompt > update-nudge > daily/quests > hero-card`.
Правила: если модалка открыта — новая встаёт в очередь и показывается по закрытию. **Тосты и барки — НЕ модалки** (идут поверх, не блокируют, не встают в очередь).
Форк: **(A) строгая FIFO-по-приоритету** (ничего не прерывает — безопасно) vs **(B) приоритет-прерывание** (высокий прерывает низкий, низкий возвращается в очередь — отзывчивее, сложнее). Реко: **A**, с одним исключением — **boss-offer показывается немедленно** (привязан к геймплей-таймингу).

### 3.2 Навигационная оболочка (решает P2 и P4)
Форк:
- **A) Нижний таб-бар.** Привычно, масштабируемо. Минус: ест дефицитную вертикаль (низ забит) — плохо для нас.
- **B) Боковой док иконок-кнопок** (вертикальная лента круглых иконок у правого края арены: Заказы / Афиша / Коллекция / Сундук / Настройки) с бейджами «!». Не трогает вертикальный стек, растёт добавлением иконок, держит нотификации. Клад уже висит верх-право арены — док встаёт туда же логично. ← типовое решение idle (Tap Titans).
- **C) Гибрид:** ядро-луп по центру, мета-входы в угловом кластере.
Реко: **B (боковой док)** — не крадёт вертикаль, масштабируется иконками, консолидирует разрозненные входы в одно место.

### 3.3 Кнопки — токенизированная система
Роли:
- **Primary** (действие кор-лупа: наточить/купить) — WOOD-фон + GOLD-бордер, h56, pill.
- **Secondary** (второстепенное) — SURF, h52, pill.
- **Ad** (за рекламу) — BLOOD + свечение, всегда с «▶», h56.
- **Positive** (забрать/клейм) — GREEN.
- **Icon** (навигация/док) — круглая, 64.
Состояния: normal / press-scale / disabled `#6a5f76`. Радиус: 999 для действий, 12 для карточек/модалок.

### 3.4 Валюты / TopBar
Чипы «иконка + значение», единый F_RES. Порядок: Золото → Колокола → Шарды (появится). Компактный формат больших чисел (уже есть). Тап по чипу → подсказка «что это».

### 3.5 Куда встают новые сущности (конкретно)
| Сущность | Где живёт |
|---|---|
| Дейли-квесты | Хаб «Афиша» → «Дела дня»: календарь + 3 заказа + бонус. Вход — док-иконка с бейджом «!». |
| Карточки героев | Тап по герою в рельсе → модалка карточки (через менеджер). |
| **Барки** | Пузырь над PunkSlot, отдельный НЕ-блокирующий слой (z 45), не модалка, mouse IGNORE. Живёт в «мёртвой» полосе над панком. |
| Гача / шарды | Док-иконка «Сундук»; Шарды в TopBar; reveal-модалка (высокий приоритет) + экран «Коллекция». |
| Бестиарий | Вход «Коллекция» (док). |
| Review / update / no-fill | Уже есть; review/update — через очередь модалок; no-fill — тост. |

### 3.6 Единый каркас модалки (решает P3)
Одна фабрика: 560px, WOOD/SURF рамка, **единый диммер (один alpha, напр. 0.66)**, заголовок F_TITLE-золото, тело F_BODY, кнопки внизу (Primary+Secondary), закрытие крестиком и тапом-вне. Все окна — через неё.

---

## 4. Форки на решение
1. **Навигация:** боковой док (реко) / нижний таб-бар / гибрид.
2. **Очередь модалок:** строгая FIFO+приоритет (реко) / приоритет-прерывание.
3. **Барки:** отдельный слой над панком (реко) / над героем в рельсе.

## 5. Рекомендованный порядок
Сначала — **инфраструктурный заход ПЕРЕД фичами:** ModalManager+очередь, dock-навигация, токены кнопок/цветов/шрифтов, фабрика модалки, единый диммер. Тогда квесты/карточки/гача просто «встают в систему», а не плодят разнобой. Затем — контент-фичи поверх готового каркаса.
