# Срез health «ХОЙ!»

Только необходимое. Цель: раз в запрос оценить **стор / продукт / деньги**, не тащить сырые выгрузки в чат.

## Как запускать

В чате: **«срез»**.

Агент:
1. Кладёт твои файлы в `store/health_inbox/` если прислал (или ты кладёшь сам).
2. Запускает `python tools/health_pull.py` (токен из `.env`).
3. Читает **только** `build/health_latest.md`.
4. Отвечает коротко: 3 вердикта + 1 действие. Без JSON, без повтора всех таблиц.

## Что тянется само (AppMetrica)

- Уники / новые / сессии: жизнь, 7д, 7д до (WoW)
- Длина сессии, краши
- Воронка уники: `tutorial_done`, `daily_claim`, `ad_watched`, `prestige`, `ad_failed`
- Rewarded по placement (просмотры `ad_watched`): Клад / ×2 вход / ×2 урон / +15с
- DAU/new/ret по дням за 7д
- Стор-установки vs first open — если есть CSV

Не тянуть: Logs API, города, модели, полный список событий.

## Что докидываешь ты (inbox)

Положи в `store/health_inbox/` перед «срез» (старые файлы можно стереть):

| Файл | Откуда |
|------|--------|
| 2× CSV RuStore | консоль: просмотры + установки по неделям |
| 1× xlsx РСЯ | блок 19547671, колонки показы + вознаграждение + eCPM |

Опционально в чат (не в скрипт): скрин Retention, если смотришь D7.

## Вердикт (шаблон ответа)

1. **Стор** — CR недели vs прошлый; иконка/тексты.
2. **Продукт** — тутор/новые, DAU ядро vs новички, сессия, D7 если скрин.
3. **Деньги** — показы и eCPM; не паниковать из‑за eCPM на <50 показах.
4. **Одно действие** на неделю — или «ничего не трогать».

Якоря (авг.2026): CR карточки ~2–3% уже ок vs 1%; D7~10% слабо для масштаба; eCPM 500–1000 норма; касса растёт от показов, не от ставки.
